# Agent icon

The icon path in plugin.json is an Aibo-local vector rendering of the Greek lowercase pi used to identify the Pi coding agent. The upstream Pi package does not distribute a standalone SVG logo.
