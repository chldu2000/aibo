/** Local renderer handles are separate from the public JSON protocol. */
export type { PresentationProps, MountedPresentation, WebPresentationAdapter } from '../../../packages/web-presentation/index';
