# Presentation 插件重构

目标确认于 2026-09-13。决策依据：[ADR-0008](adr/0008-unified-presentation-plugins.md)。

## 产品目标

用户通过一个皮肤插件入口安装、选择、升级、禁用和卸载 Presentation 包。同一包可以只定制主题，也可以提供控件、语义视图和整个工作台；宿主补全未提供的范围。shadcn 与 Material 3 最终作为可独立构建的包验证这条路径，宿主始终保留可用的默认呈现。

成功标准不是移动文件或改名：仓库外构建的包必须能进入实际 App，切换后生效，并且能升级、卸载和从故障中恢复。

## 必须保持的约束

- 会话、草稿、导航、历史与执行结果由宿主持有；呈现卸载不删除业务数据。
- 插件发出语义动作，宿主验证权限、上下文和代际后执行。审批及恢复入口不受可替换代码控制。
- UiKitAdapter 仍是完整内部合同；插件可以省略定制，宿主组合后不能缺少必需控件或核心语义。
- 应用层仅通过 UI kit 使用视觉实现，不按插件或皮肤 ID 分支，不引入视觉 CSS。
- 能力插件无需为换皮肤修改业务协议。新业务数据需求单独进行兼容演进。
- 不把同 WebView 执行或 CSS 作用域宣称为安全隔离。外部可执行包上线前必须确定信任模型及验证证据。

## 实施阶段与验收

### P0：统一术语与内置注册

- [x] 记录统一产品概念、ADR 与本实施文档。
- [x] 建立本地 Presentation 定义与默认继承，现有外观入口消费统一注册。
- [x] 验证主题独立定制、控件继承与不合法主题拒绝；保持双皮肤构建及架构门禁通过。

### P1：稳定包合同与加载设计

- [x] 定义版本化纯数据 manifest、包身份、入口、资源清单、完整性和宿主兼容声明。
- [x] 区分发布版本、可执行接口版本、核心语义版本和快照版本。
- [x] 决定可执行代码的信任与隔离方式，覆盖网络、文件、IPC、样式及资源权限；记录 ADR。
- [x] 定义主题、控件、renderer、工作台的缺省继承及兼容规则，不增加第二插件类型。

### P2：宿主安装与运行生命周期

- [ ] 实现外部包发现、验证、安装、启用、切换、禁用、卸载与升级。
- [ ] 候选加载成功后提交选择；失败恢复上一可用版本或内置默认呈现。
- [ ] 单一选择驱动主题、控件和工作台；持久化选择并迁移旧外观设置。
- [ ] 处理迟到动作、资源释放、重复切换、重启、丢失包和损坏包。
- [ ] 固定管理、审批与恢复区域在恶意或故障呈现下保持可达。

### P3：迁移内置实现并开放外部开发

- [ ] shadcn 与 Material 3 迁为独立构建包，提取资源与样式并消除宿主源码依赖。
- [ ] 提供纯主题和完整呈现两种样例，使用同一包合同和安装入口。
- [ ] 完成开发文档、打包工具与仓库外构建、安装、展示、升级、卸载验收。

### P4：回归与退出审计

- [ ] 实际 App 验证双皮肤、布局、核心语义、键盘、焦点、滚动、草稿和会话保留。
- [ ] 验证不兼容、初始化失败、运行故障、升级失败、卸载和重启恢复。
- [ ] 每次交付运行 `pnpm run verify`；涉及原生生命周期时补充对应 Rust 与真实 App 检查。
- [ ] 更新 UI 架构与插件支持矩阵，注明实际平台证据，不将内置模块注册当作外部加载完成。

## 当前状态

P0 已完成：`src/lib/ui-kit/presentation-plugin.ts` 提供可信本地定义与默认继承，
registry 将现有两套皮肤解析为统一注册，PresentationSurface 从当前注册读取
renderer 描述符。旧外观选择与存储键继续兼容；工作台槽位及专业 renderer 的
可执行实现仍由宿主内置，尚未转成外部扩展。

2026-09-13：`pnpm run verify` 通过（25 项架构检查、189 项 Node 测试、类型检查、
生产构建）。新增 3 项测试覆盖主题继承、部分控件覆盖、非法注册拒绝。
构建保留既有主 chunk 超过 500 kB 提示。本次未运行真实 App 视觉或状态恢复探针。

P0 提交：`3310e2a`。

P1 定义见 [包合同](presentation-package.md) 和 [隔离决策](adr/0009-presentation-package-isolation.md)。
实现包括 JSON schema、公共纯数据类型、无运行时编译的生成验证器和实际资源
长度/SHA-256 校验。默认继承与版本含义已明确；隔离决策的实际安全性和可用性
仍须 P2–P4 验证，不能凭 ADR 宣称完成执行隔离。

P1 验证：`pnpm run verify` 通过（25 项架构检查、194 项 Node 测试、类型检查、
生产构建）。公共协议 tarball 在仓库外无 DOM 编译包含新 manifest 类型；新增
5 项测试验证包格式、资源摘要与长度、路径/大小限制、主题注入拒绝和生成器一致性。
一次全量运行的 Pi capability workflow 报 Invalid control identity or operation，
定向重跑及随后完整 verify 均通过；本阶段未宣称修复该偶发现象。保留主 chunk
大小提示，未运行原生 App，原生安装、符号链接防护和沙箱探针仍待 P2–P4。

P1 提交：`d594cb0`。

P2 正在实施，按原生存储（P2a）、隔离运行与恢复（P2b）、App 入口与统一选择
（P2c）分批提交，三批完成后才勾选 P2 退出项。

P2a 实现：`presentation_packages.rs` 和迁移 0042 持有不可变 release、启用状态和
按窗口保存的选择。原生安装先验证 manifest、资源上限、实际长度和 SHA-256，
拒绝符号链接和同 ID/版本不同内容；读取时再次验证。安装不改变选择，选择提交
检查 expected digest，禁用/卸载事务清除受影响选择。卸载保留不可变资源用于
重装恢复，不删除业务数据。Tauri 命令及前端类型端口已接入，尚无 App 管理入口。

P2a 的磁盘数据库测试覆盖安装后未激活、窗口隔离、候选版本不覆盖旧选择、过期
提交拒绝、重开数据库、禁用、卸载和重装；另有损坏包、版本冲突、路径和符号链接
拒绝测试。它们不代替外部脚本执行、实际 App 和跨平台验收。

P2a 验证：`pnpm run verify` 通过（194 项 Node 测试、25 项架构检查、类型与
构建），完整 `cargo test --manifest-path src-tauri/Cargo.toml --lib` 通过
（199 项）。首次沙箱内运行 198 通过、1 项进程取消测试因 `ps` 被系统拒绝失败；
经自动审批在沙箱外重跑全量后 199 通过，未修改该测试。保留既有构建体积和
Rust 警告。原生测试平台为 macOS arm64，尚未启动实际 App 验收本批命令。

下一步是 P2b 隔离运行与恢复，再接 P2c 的产品入口；当前不能从设置安装或执行
外部呈现。上一轮插件平台退出审计继续描述上一轮成果，本轮进度以本文为准。
