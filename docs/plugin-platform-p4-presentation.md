# P4 第一批：呈现描述与降级协商

默认工作台和语义视图的预检已接入可信 default-presentation 模块。描述符明确核心语义版本、四种必需语义和可选专业呈现；缺少核心类型、未知版本、重复或越界的可选声明均拒绝预检。

专业呈现按 ID、版本及核心类型匹配，匹配失败退回当前 renderer 的核心视图。两条路径接收完全相同的规范快照副本，不能通过替换成缩减数据来隐藏必需字段或动作。非法语义快照直接拒绝，不伪装成可降级内容。

本批没有实现实际专业 renderer，也没有把默认 App 的全部布局拆成槽位。P4 剩余的布局装配、专业呈现实例、独立可信恢复入口、写入审批及副作用验收继续推进；此批只作为可独立提交的合同基础。

| 边界 | 当前支持与所有者 |
| --- | --- |
| 默认呈现实现 | dev.aibo.ui-default 1.0.0，构建期可信模块 |
| 核心语义契约 | 1.0.0，四种核心语义全部必需；旧实验数据继续独立读取 |
| 可选专业呈现 | 精确 ID/版本/核心类型匹配；默认实现暂不声明专业类型 |
| 布局与视觉 | 布局归表现插件；控件视觉归当前 skin，业务模块不获样式权 |
| 执行与恢复 | 宿主拥有状态、审批和恢复；描述符不授予新的执行权限 |

描述符预检独立于完整语义验证器，避免工作台首屏为了检查版本而加载整个语义 schema；完整快照校验仍在语义视图加载时执行。

验证：pnpm run verify 覆盖 23 项架构检查、143 项 Node 测试、类型检查和生产构建；新增 renderer-negotiation 测试验证核心要求、专业版本不匹配、完整数据/动作保留和类型约束。架构测试现已递归扫描 workbench 子目录，没有降低原有规则。

node probes/semantic-ui-browser.mjs 通过两套皮肤和两种 renderer 的显示、动作、焦点及恢复回归。本批没有 Rust 改动。

### 宿主恢复入口（第二批）

工作台在可替换内容之外始终提供「恢复默认呈现」按钮，并在窗口捕获阶段响应
Ctrl/Cmd + Shift + Backspace。恢复请求可以覆盖正在排队的布局切换；旧切换的
完成回调不能清除新请求的 busy 状态，过期挂载不能写入布局偏好或恢复焦点。
错误提示仅报告「呈现错误」，避免在默认呈现也失败时误报已经恢复。

验证：`pnpm run verify` 通过；`node probes/workbench-recovery-browser.mjs`
使用真实 WorkbenchPresentation 验证常驻按钮、快捷键、挂载失败回退和再次恢复。
此批仅完成恢复控制路径；插件管理、授权、历史的独立宿主区域及完整布局装配仍待完成。

### 独立宿主区域（第三批）

标题栏、插件管理、设置与诊断移出可替换工作台。插件面板不再因为呈现切换而
重新挂载；恢复控件在插件管理打开时仍可操作。工作台暂时隐藏但保持实例，
关闭管理后继续显示。插件操作捕获宿主工作区/会话上下文，拒绝旧上下文回调；
不再依赖 renderer generation。`workbench-contract.ts` 仍只描述可替换区域的
快照与动作，宿主控件不会作为插件可调用的动作注册进去。

授权、独立历史浏览与完整布局装配尚未完成，本批不据此勾选整项 P4 验收。

第三批验证：`pnpm run verify`；`node probes/git-plugin-native.mjs` 的两次真实
桌面启动均通过（含双皮肤宿主管理实例保留、Git 视图与重启恢复）；
`node probes/host-shell-browser.mjs` 使用真实 App 与鼠标点击验证双皮肤入口可达，
没有以 DOM `.click()` 代替遮挡检查。隐藏工作台的动作通道也暂停，防止迟到的
界面回调在管理区域打开时继续触发。

### 独立宿主审批（第四批）

Agent 审批从时间线移入宿主区域，显示所有待审批会话及请求内容。可替换工作台
不再获得审批决定回调。插件管理打开、呈现切换及后台会话审批均使用同一宿主
区域。审批控制器拒绝失效/内容改变的请求和重复提交；迟到响应不会删除同键但
属于新 turn 的替代请求。原生宿主仍执行最终有效性检查。

本批是 Agent 审批入口隔离，不等同于完成 Capability 写入审批或非 never 的
PluginView 动作确认；这些通路、独立历史及完整布局装配仍在 P4 待办中。

第四批验证：完整 `pnpm run verify` 通过（23 项架构检查、144 项 Node 测试、
类型与构建）。`node probes/p2-native.mjs` 两次真实桌面启动通过；实际 Codex 与
Pi 请求均通过宿主按钮批准，覆盖后台会话、插件管理同时打开、审批区域跨呈现
切换保留及批准后完成。Echo 流式切换、草稿、焦点、Agent 不重启、真实进程重启
后的模型配置/历史和双窗口隔离回归也通过。

### PluginView 原生确认（第五批）

非 `never` 动作由 Rust 宿主打开原生确认对话框，展示插件、会话、工作区、
视图、动作、能力和本次参数。插件 DOM 不获得“已确认”标志或免确认 token。
`when-sensitive` 暂与 `always` 一样要求确认：目前没有足以安全跳过确认的
通用敏感性分类。取消、对话框不可用或超过五分钟均不执行。

输入 schema/manifest/capability 先通过校验，再请求确认。确认后重新核对当前
视图文档、runtime generation、会话状态、安装版本、包摘要、执行配置、启用/
安装状态及工作区信任；变化时拒绝执行，不自动重试。一次只接纳一个 PluginView
确认/派发，重复请求返回 busy；取消任务会释放并发许可。该门仅覆盖 PluginView
动作，不把它等同于未来 Broker 写入任务的幂等、审计与结果未知协议。
动作输入上限为 16 KiB，确认时完整显示参数，不截断后批准隐藏部分。

真实 Echo 子进程测试覆盖 never 兼容、always 拒绝、when-sensitive 批准、重复
提交、无桌面确认端口、确认期间信任撤销和视图变化。原生对话框的人工点击尚未
验收；旧 v1 IPC 尚不携带调用者所见的 revision/generation，当前复核比较的是
宿主本次确认前后的上下文。因此确认清单仍不勾选完成，后续补 caller freshness
和原生对话框验收，不将此批视为整个 P4 审批完成。

第五批验证命令：`cargo test --manifest-path src-tauri/Cargo.toml`（140 通过），
`pnpm run verify`（23 项架构检查、144 项 Node 测试、类型与构建通过）。

### 调用者视图版本（第六批）

宿主新增 `get_plugin_view_snapshots`，从同一行读取 v1 文档及 generation，并
以 `{ document, version: { generationId, revision } }` 返回。插件自身的文档
协议保持不变。前端保存该快照，将对应版本原样传入动作 IPC；缺少版本不能调用。
原生宿主在动作确认前核对 revision/generation，拒绝旧视图、旧运行实例的动作；
确认后的整份上下文检查继续生效。测试专用的无版本调用不编译进生产接口。

第五批提到的 caller freshness 缺口由此补齐；原生确认对话框点击验收仍待完成。

第六批验证：完整 Rust 测试 140 通过；`pnpm run verify` 的 23 项架构检查、
144 项 Node 测试、类型与构建通过。`node probes/p2-native.mjs` 两次真实桌面
启动通过：实际插件视图按钮完成调用，原生 IPC 拒绝旧 revision、旧 generation
及缺少版本的请求；Codex/Pi 审批、重启恢复和窗口隔离回归通过。

### 原生对话框验收（第七批）

动作 IPC 由 Tauri 注入发起调用的 WebviewWindow，宿主将它设置为原生确认的
父窗口。该窗口不是插件提供的参数。没有发起窗口时不打开独立的系统级通知
对话框；这一点使多窗口确认的归属明确。macOS 库的无父窗口分支使用系统通知，
有父窗口分支使用应用内原生 sheet，本批用实际运行验证并切换到后者。

`node probes/view-confirmation-native.mjs` 创建隔离工作区及 Echo 安装包，将测试
动作声明为 always，通过真实 IPC 发起确认。Swift 辅助功能工具只在该隔离进程
中定位包含指定测试标记的对话框，实际点击取消和批准。取消返回
confirmation_cancelled 且不更新视图；批准返回操作结果并更新视图。没有模拟
确认返回值，也没有操作用户的其他应用窗口。此 probe 适用于已授予辅助功能
权限的 macOS；其他平台的原生对话框外观不据此宣称已经验收。

结合第五批真实子进程的拒绝、信任撤销、上下文变化和重复提交检查，以及第六批
真实 IPC 的 revision/generation 检查，PluginView confirmation 清单项现已完成。
[原生按钮验收结果](./baselines/plugin-platform-p4/view-dialog.json)仅包含布尔结果
和按钮名称。完整 P4 的布局、独立历史、专业呈现和 Broker 写入仍待完成。

第七批完整回归：`cargo test --manifest-path src-tauri/Cargo.toml` 140 通过；
`pnpm run verify` 的 23 项架构检查、144 项 Node 测试、类型检查及构建通过。

### 项目任务领域服务（第八批）

项目任务的定义校验、保存/删除/查询、执行与历史读取从 `lib.rs` 提取到
`src-tauri/src/project_actions.rs`。五个 IPC 命令只转发参数；服务依赖数据库和
artifact 根目录，不接收 AppState、窗口或 Tauri State，也不调用 Tauri runtime。
因此后续受控执行入口可以复用同一份领域逻辑，不需要再复制 IPC 实现。

保留既有信任检查、会话所属工作区校验、cwd 边界、进程组终止、输出限额和脱敏。
直接服务测试使用隔离数据库与真实子进程，覆盖保存/查询、stdout/stderr、持久
运行记录、禁止 cwd 越界，以及撤销信任后拒绝执行且不新增运行记录。

本批是接入 Broker 写入前的项目任务服务提取。Git 服务仍待提取；旧执行语义
仍在执行结束后记录结果、超时标为 timed_out、删除定义会级联删除运行记录。
后续持久任务接纳、审批、幂等、取消、结果未知与历史保留必须独立实现，不能
以当前服务可调用代替 P4 写入验收。相关整项清单继续保持待办。

第八批验证：完整 Rust 测试 141 通过；`pnpm run verify` 的 23 项架构检查、
144 项 Node 测试、类型及构建通过。

### 工作区 Git 领域服务（第九批）

工作区 Git 的索引操作、提交、分支、提交历史/文件差异、远端状态/同步及 stash
迁入 `src-tauri/src/workspace_git.rs`。对应 IPC 命令只转发数据库和语义参数，
不再包含 Git 命令装配及结果解析。服务不接收窗口、AppState 或 Tauri State。
Core 的 turn 基线恢复仍留在原有恢复流程，索引更新复用提取后的同一函数。

直接服务集成测试在隔离仓库中执行暂存、提交、建分支、stash 保存/应用，并核对
Git 自身的索引、历史与文件内容；越界路径、选项式分支名被拒绝，撤销工作区信任
后写操作被拒绝。测试禁用临时仓库的 hooks 和提交签名，不修改用户仓库配置。

项目任务与工作区 Git 的服务提取已具备直接调用证据，但这还不是 Broker 写入
开放：同步 Git 命令的执行限额、持久接纳、审批、写冲突、取消、结果未知与审计
仍需接入受控执行层。相关 P4 整项清单继续保持待办。

第九批验证：完整 Rust 测试 142 通过；`pnpm run verify` 的 23 项架构检查、
144 项 Node 测试、类型及构建通过。

### 有界进程执行（第十批）

新增 `controlled_process`，项目任务使用该执行器。退出等待和 stdout/stderr 回收
共用一个期限：主进程退出而子进程继续占用管道时，不再无限等待输出。保留期限
内读到的部分输出，单流内存有上限，超额数据仍持续排空，避免反压卡住进程。
结束、超时或执行 Future 被取消时清理进程组；退出后的回收等待也有期限。

Unix 真实进程测试覆盖“主进程退出、子进程持有管道”、大量输出及取消后子进程
不得继续写文件。Windows 保留 taskkill 清理路径，尚未做 Windows 原生验收。
此执行器约束进程生命周期，不是文件系统/网络沙箱。项目任务 v1 暂继续返回
原有 timed_out 状态，持久接纳、确认、取消入口、结果未知和 Broker 写入仍待接入。

首次完整 Rust 运行中新增三项进程测试通过；既有 PluginHost 综合测试出现一次
idle/interrupted 状态断言失败，与此前基线记录同形。该次失败保留为回归记录，
不因随后复跑通过就宣称其原因已经修复。

第十批最终验证：完整 Rust 复跑 145 通过；`pnpm run verify` 的 23 项架构检查、
144 项 Node 测试、类型及构建通过。首次既有 PluginHost 状态失败的原因仍未确认。

### 项目任务持久运行记录（第十一批）

迁移 0029 在执行前写入 running 记录与任务定义快照，完成后更新同一 run ID。
新记录为 `aibo.project-action-run/v2`，运行期间 completedAt 为 null。超时、执行
错误及启动恢复发现的未结束记录标为 outcome_unknown，不自动重放。当前执行器
错误尚未区分 spawn 前后，所以错误分支保守标为结果未知；后续可在保留证据的
前提下细分明确未开始的失败，不能默认认为所有错误都没有副作用。

移除任务定义与运行记录之间的级联删除，保留任务名称，删除定义后仍可读历史。
旧 v1 记录保持原 schema、输出和时间。迁移补充的定义明确标为
migration-current-definition，不能当作旧执行时的完整快照；新记录的 admission
快照才表示本次接纳时的定义。工作区删除仍按原数据清理规则删除该工作区记录。

启动恢复只将 running 转为结果未知，不重试，重复恢复不重复修改终态。终态更新
要求记录仍为 running，避免覆盖并发恢复结果。UI 显示“执行中”和“结果未知”，
且晚到结果不会混入用户已经切换到的其他工作区。

直接服务测试覆盖执行期间记录可见、取消执行 owner 后关闭并重开数据库、恢复
幂等、删除定义后保留名称和记录，以及执行错误仍留下可读取结果；独立迁移测试
证明旧 v1 记录不丢失。本批仍未接入审批、用户取消入口、幂等键或写冲突门，P4
受控写入整项保持待办。

本批验证还定位到既有 PluginHost 状态偶发失败的一条明确路径：日志捕获 SQLite
错误 517（BUSY_SNAPSHOT），事件投影因此停止插件并将会话中断。投影事务改为
BEGIN IMMEDIATE，在读取状态前预留写锁，避免与其他连接写入之间的事务升级
冲突。新增并发测试验证其他写入必须等投影提交，且两次更新均保留。没有通过
重放插件命令来规避争用；不能据此断言此前所有同形历史失败都只有这一种原因。

第十一批最终验证：完整 Rust 测试 148 通过；`pnpm run verify` 的 23 项架构检查、
144 项 Node 测试、类型及构建通过。投影争用修复单独提交，任务记录与迁移另行提交。

### 项目任务持久请求身份（第十二批）

迁移 0030 为任务记录增加 request_id 与独立 request_json，并以
(workspace_id, request_id) 建立唯一约束。旧记录保留空请求身份，不参与去重。
宿主 IPC 要求请求 ID；前端 API 默认为新执行生成 UUID，也允许调用者显式保留
原 ID 查询同一请求。ID 限制为 1–128 个 ASCII 字母、数字、连字符或下划线。

同一工作区、同一请求 ID 的并发提交只有数据库接纳成功的一次可以启动进程。
重复提交返回已有 running 或终态记录，不等待或重新执行；同一 ID 改换任务或
session 则拒绝。输入身份独立保存，避免 session 删除后的外键置空改变去重含义。
重放先检查工作区存在且仍受信任，然后读取原记录，不重新解释已编辑或删除的
任务定义。因此重复请求不会因任务修改执行新命令；需要新执行时必须使用新 ID。
工作区删除仍按原规则清理记录，此去重保证限于记录保留期间。

直接服务测试用真实 shell 追加文件，验证并发相同请求只产生一次副作用、返回
同一运行 ID、不同输入不能复用 ID、删除定义并重开数据库后仍返回原结果。
中断恢复测试额外验证重放 outcome_unknown 仍返回原记录，不启动进程。

本批只是受控写入的持久去重基础。每次独立 UI 点击当前仍生成新 ID，尚未实现
按钮级重复点击合并；不同请求之间的工作区写冲突、调用者身份传播、审批、用户
取消和 Git 写入接入仍待完成，不据此勾选 P4 的完整写入验收。

第十二批验证：完整 Rust 测试 149 通过；`pnpm run verify` 的 23 项架构检查、
144 项 Node 测试、类型及构建通过。

### 工作区写入互斥与任务重复点击（第十三批）

新增 workspace_writes 宿主领域模块，以规范化工作区路径持有进程内写入许可。
项目任务和工作区 Git 的文件暂存/取消暂存、整体暂存、提交、分支切换/创建、
fetch/pull/push、stash 保存/应用共用许可，冲突立即返回 workspace_write_busy，
不排队、不自动重试。许可覆盖执行与结果保存阶段，退出或 future 丢弃时释放。
许可获取后还检查持久 running 任务，避免任务 owner 丢失时绕过未恢复的记录。

任务接纳阶段使用短期互斥，涵盖重复请求查询、许可获取与 intent 保存，执行前
释放接纳互斥。因此同一请求能观察到已接纳记录；不同请求不能同时开始。不同
工作区仅短暂串行接纳，实际任务可以同时执行。已有 running 记录需启动恢复后
才解除阻挡；恢复为 outcome_unknown 不触发重试，后续新执行需新请求。

前端项目任务控制器由 App 持有，独立于可替换 renderer。同一工作区、任务与
session 的未结束点击共用一个 Promise 和请求 ID；完成后再次点击才创建新请求。
不同工作区/session 不共用请求。失败向调用方传播，不自动提交下一次执行。

真实 shell/临时 Git 仓库测试覆盖任务阻止同工作区任务及 Git 暂存、另一工作区
任务继续、完成后 Git 能暂存、持有 Git 侧许可时任务不能接纳，以及任务 owner
丢失后持久 running 记录继续阻止写入。前端测试覆盖连点合并、作用域区分、明确
再次执行生成新 ID 和错误不自动重试。

范围仍为同一宿主进程内的项目任务与 workspace_git 服务，不是文件系统沙箱或
跨进程锁，不约束外部终端、Agent 自行执行的命令、嵌套工作区或旧 turn 恢复路径。
Git 的完整持久执行记录、进程树取消/超时结算、审批及通用 Broker 写入仍待接入。
本批互斥不意味着 Git 已具备任务一样的重启恢复或结果未知保证，P4 完整写入
验收继续保持未完成。

本批首次全量 Rust 测试为 149 通过、1 失败：既有 PluginHost 综合用例在刷新能力
返回后检查非法输入，未得到预期的 input schema 错误。增加错误文本诊断后，单次
及连续 10 次独立运行均通过，未捕获首次错误的具体文本。代码检查发现刷新响应
不代表异步视图投影完成，故测试补上已有 settled_view 条件等待，再执行原校验
断言；不放宽错误要求或宿主过期检查，也不将未重现的首次失败断言为已确定根因。

第十三批最终验证：完整 Rust 测试 150 通过；`pnpm run verify` 的 23 项架构检查、
145 项 Node 测试、类型及构建通过。

### 项目任务主动停止（第十四批）

迁移 0031 为任务记录增加 cancel_requested_at。cancel_project_action IPC 根据
宿主记录的 workspace/run 身份保存停止请求，重复请求保留第一次时间；已结束或
不属于该工作区的记录返回 false。停止已有任务不要求工作区继续受信任，避免撤销
信任后反而无法终止执行。该入口目前仅供可信桌面宿主使用，尚未对插件 Broker
开放；插件调用者身份传播、审批与通用写入权限仍须在后续闭环中接入。

受控进程执行器新增取消 future，与整个进程及输出管道的共同 deadline 竞争。
任务每 100ms 读取持久停止请求，收到后终止进程组、有界回收并保留已捕获输出。
记录消失或数据库读取失败也触发停止，避免继续不可观测的写入；数据库故障可能
使最终记录更新失败，此时按已有启动恢复规则标记结果未知。取消请求被接受只
代表 intent 已保存，不代表进程已经退出；写入许可直到执行结算后才释放。

取消后 status 为 outcome_unknown，输出解释已有副作用可能保留，不能表示已
回滚。若进程正常完成先被执行器观察到，则保留真实完成结果；停止请求时间仍在
记录中。重复原 request ID 返回同一终态，不重新运行。当前 stdout/stderr 仍在
结算时保存，没有宣称支持实时日志或保证未读取的管道尾部全部留存。

工程动作运行列表增加带任务名的“停止”按钮，通过 Inspector 和 App 的 generation
守卫转发。App 在上下文面板可见时读取持久历史，读请求不重叠，离开工作区或关闭
面板后丢弃迟到结果；读取失败保留现有记录并继续观察。停止观察不取消执行。
已知终态不被较早发出的 running 查询覆盖，工作区重新进入可重新看到运行记录。

直接服务测试使用真实 shell，覆盖跨工作区/不存在记录拒绝、撤销信任后仍可停止、
重复停止保留首次时间、后代延迟写入终止、先前文件与输出保留、同请求不重放及
结算后写入许可释放。前端测试覆盖停止观察后迟到结果不发布及临时读取错误恢复。
新增 project-task-cancel-native 桌面探针，使用实际组件按钮与原生 IPC，分别在两套
皮肤执行任务、撤销信任、停止并检查持久结果与真实文件副作用；它验证任务面板
和 IPC，并非完整 App 导航或所有平台的证明。

第十四批验证：完整 Rust 测试 151 通过；`pnpm run verify` 的 23 项架构检查、
146 项 Node 测试、类型及构建通过。macOS 原生任务面板探针在 shadcn/Material 3
均通过，证据见 [任务停止验收](./baselines/plugin-platform-p4/task-cancellation.json)。
探针完成后主动结束隔离 App，子 pnpm 因此打印 ELIFECYCLE；外层探针退出码为 0，
且报告包含两套皮肤的实际运行 ID、持久结果及文件副作用检查。

本批未完成项目任务审批、完整独立宿主任务/历史入口、Git 持久写入与取消、
通用 Broker 写入接纳，P4 整体仍未完成。

### 项目任务原生审批（第十五批）

迁移 0032 扩展任务生命周期：新执行使用 `aibo.project-action-run/v3`，先保存
awaiting_approval，批准且复查通过后才原子转为 running。拒绝、不可用、审批超时、
取消或上下文失效转为 rejected，并保存 approval_outcome、approval_decided_at 与
未执行原因；原 v1/v2 记录、请求身份、取消时间、输出及快照原样迁移，不补造审批。
等待审批时重启可以明确恢复为“未执行”，running 时重启仍只能判断为结果未知。

生产 IPC 从 Tauri 注入调用窗口，绑定该窗口的原生确认对话框，不接受前端传来的
批准布尔值。摘要包含任务名、规范化工作区/工作目录、程序、完整 JSON argv、
会话与窗口；摘要超过 16 KiB 时拒绝接纳，避免静默截断用户需要审查的命令。
审批等待上限 5 分钟，等待期间也响应持久取消；窗口回调丢失或错误不能放行。
超时/取消后迟到的原生批准回调没有有效接收者，不会重新执行；系统对话框仍可能
需要用户关闭。测试中的自动批准入口仅在 cfg(test) 编译，生产路径必须传宿主
确认端口。

审批前冻结完整任务定义、工作区路径/信任/更新时间、规范化 cwd、会话状态与
更新时间。批准后重新读取和比较，上下文有变化则拒绝该请求，不替换成新命令，
也不自动重新审批。工作区写入许可从接纳持续到审批及执行结算，避免另一项受控
写入在审批期间进入。同一请求的重复提交只读现有记录，不再次弹窗。
新请求身份包含宿主窗口 label，同一 ID 换窗口或输入会被拒绝；旧版无窗口身份
的请求仍允许按原输入读取已有结果，不升级成新的审批或执行。

运行列表显示“等待宿主批准”和“未执行”，等待批准也可请求停止；观察历史时
按 awaiting_approval → running → 终态的顺序拒绝较早查询造成的状态倒退。
这些检查不构成 OS 沙箱：外部程序、可执行文件内容或环境的变化不在冻结合同内，
路径复查与启动之间也不宣称消除了所有文件系统竞争。Git 写入尚未复用此审批流程。

直接服务测试覆盖原生端口收到冻结摘要、批准前无副作用、拒绝及窗口不可用、
修改 argv/撤销信任后的失效拒绝、等待审批时取消、批准后真实执行、审批结果持久
保存、重复请求不重复审批、窗口身份冲突、等待超时和重启恢复。增量迁移测试
覆盖旧记录字段保留；旧版无 caller 的请求重放验证仍返回已有结果。
桌面任务探针增加真实原生“取消”和“允许本次执行”按钮自动化，并继续验证两套
皮肤的任务停止按钮。AX 辅助程序只查找隔离 App PID 下的指定标题与任务标记。

第十五批验证：完整 Rust 测试 154 通过；`pnpm run verify` 的 23 项架构检查、
146 项 Node 测试、类型及构建通过。macOS 桌面探针实际点击一次原生拒绝、两次
原生批准，并在两套皮肤中停止获批任务，验证拒绝无文件副作用、停止保留已有
更改且阻止后代延迟写入。证据见 [任务审批验收](./baselines/plugin-platform-p4/task-approval.json)。
外层探针退出码 0；清理隔离 App 产生的子 pnpm ELIFECYCLE 与上一批含义相同。
项目任务审批已接入，Git 持久写入/审批/取消、通用 Broker 写入与独立宿主历史入口
仍待完成，P4 整体验收不作完成标记。

### Git 同步有界执行（第十六批）

workspace_git 的 fetch/pull/push 从直接 Command.output 加外层 timeout，改为复用
controlled_process。120 秒期限同时覆盖根进程、后代持有的输出管道及输出读取；
结束/超时/owner 丢弃时清理进程组，有界回收根进程。保留原有工作区信任与写入
互斥、明确的 Git argv，以及禁用终端凭据交互的环境约束，不自动重试。

stdout/stderr 各最多保留 256 KiB + 1 字节并继续排空超出内容；合并后按已有命令
输出规则处理敏感字段，最终响应限制为 256 KiB，截断时有标记。正常返回也保留
stderr 中的 Git 进度/结果信息。明确的非零退出继续返回 applied=false；它表示
同步未成功，不证明远端或所有本地引用都未发生任何变化。

超时或执行器错误返回结构化 outcome_unknown，提示先核对实际更改；超时响应
保留已捕获输出，不把未获得完整结果解释为可以直接重试。spawn/读取错误当前
同样保守归为未知，尚未细分确定未开始的失败。这个结果仍通过当前 IPC 返回，
本批不宣称 Git 已有持久运行记录、审批、用户取消入口或重启恢复保证。

测试使用临时本地裸仓库，走实际服务入口验证 push/fetch/pull，并比对远端分支与
本地 HEAD；不访问网络或用户仓库。另用本地 SSH 替身驱动真实 git fetch，验证
持有管道的后代触发期限、保留部分输出、错误码为 outcome_unknown、后代延迟写入
被终止；噪声输出测试覆盖截断与敏感字段处理。替身通过 GIT_SSH/GIT_SSH_VARIANT
显式注入，不进行真实 SSH 连接。

本批仅迁移远程同步执行路径。暂存、提交、分支和 stash 等其余写入命令还需要
接入相同执行管理；Git 的持久接纳、审批、取消和通用 Broker 接入仍是 P4 待办。
进程树清理本批在 Unix/macOS 验证，不据此声明 Windows 或外部逃逸进程已被约束。

本批验证首次出现两个问题：全量测试中 200ms 的超时用例未捕获替身输出标记，
独立复跑能捕获该标记，说明该短期限不能稳定作为 Git 已完成启动的前提；测试
改为 2 秒期限、4 秒后的后代副作用，并仍要求外层 5 秒内返回及后代不产生文件。
另一个确定缺陷是脱敏会缩短文本，导致已被执行器截掉的尾部可能漏报“已截断”。
实现改为在脱敏前记录原始捕获是否超限，再保留显式截断标记；测试同时验证敏感
样例值不泄漏、标记存在及最终大小上限。没有通过删除相关断言完成验收。

随后一次全量运行的 Git 用例均通过，但既有 Broker 父期限传播用例只有 crash 阶段
的 cancelled 子记录，没有 timeout 阶段的子记录。该用例原 700ms 包含冷启动，
可能只测到父调用在子调用启动前到期。测试改为 3 秒父期限、5 秒子延迟，并在
等待超时结果前明确确认父子均已运行；仍要求 timeout/cancelled 两条子终态记录、
无 running 残留及无 flight 泄漏，未修改 Broker 的生产期限或取消语义。

第十六批最终验证：完整 Rust 测试 155 通过；`pnpm run verify` 的 23 项架构检查、
146 项 Node 测试、类型及构建通过。Broker 测试时序修正单独提交；Git 同步受控
执行、错误语义与本批实施记录另行提交。没有对用户仓库执行同步或连接真实远端。

### 其余 Git 写入有界执行（第十七批）

暂存/取消暂存、整体暂存/取消暂存、提交、分支切换/创建、stash 保存/应用全部
迁入与同步相同的受控异步执行路径。GitOperation 为一次操作的前置检查、写入
及后置读取提供同一个 120 秒截止时间，每个子命令只拿剩余时间；过期后不再启动
下一条命令。输出上限、敏感字段处理、截断标记、进程组清理和 outcome_unknown
语义复用第十六批实现，去掉各写入口重复的阻塞 Command.output 处理。

取消暂存先用 rev-parse --verify --quiet 明确区分存在 HEAD、尚无 HEAD 与检查
故障；提交前检查只把 diff --cached --quiet 的退出码 1 解释为有差异，其他错误
不进入提交。成功提交后的可选 hash 读取失败仍返回 committed=true、hash=null，
避免用户因附加读取失败误以为提交未发生。提交钩子未结束时超时则为结果未知，
不能依据命令尚未返回断言 commit 不存在。

共用命令构造入口启用 --literal-pathspecs，文件操作中的输入始终表示字面路径。
这堵住了嵌套工作区中将 :(top) 等表达式解释为仓库根路径的通道，同时支持实际
包含类似表达式或星号的文件名。原路径范围和分支参数检查继续保留。

Core 的整文件 stage/unstage 也改为 await 共用索引执行器，并在进入索引操作前
取得同一工作区写入许可，不能绕过项目任务互斥。Core 的 hunk、整文件基线恢复
和其他 turn 恢复路径仍未迁移到本模块，不据此声明所有历史写入口均已统一。
独立只读查询仍保留原实现，本批没有扩展其验收范围。

新增真实 Git 测试验证无 HEAD 时取消暂存不删除工作文件、前置检查故障不启动
提交、过期操作不启动下一条写命令、嵌套工作区不能通过路径表达式暂存外部文件。
提交钩子测试验证 post-commit 卡住时返回结果未知，即使 commit 已实际写入；
后代延迟副作用被终止且没有自动追加第二次提交。原服务回归补充实际分支往返，
继续覆盖提交、stash、push/fetch/pull、作用域与信任约束。

Git 持久接纳、审批、主动取消和通用 Broker 写入仍待接入。当前有界执行器提供
这些功能所需的统一进程边界，但没有把当前 Git IPC 返回值宣称为持久执行历史。

第十七批最终验证：完整 Rust 测试 157 通过；`pnpm run verify` 的 23 项架构检查、
146 项 Node 测试、类型及构建通过。所有 Git 写入与钩子验证均在临时仓库进行；
路径表达式/特殊文件名和进程组用例在 Unix/macOS 验证，未声明 Windows 实测支持。

### Git 持久执行记录（第十八批）

迁移 0033 建立宿主持有的 workspace_write_runs。工作区 Git 的索引、整体索引、
提交、分支、同步与 stash 服务，以及 Core 整文件 stage/unstage，统一通过
workspace_write_runs::execute：先取得工作区许可，持久保存 operation、工作区
路径与完整输入，再构造/等待执行 future，最后更新同一 run ID。意图超过 64 KiB
在执行前拒绝，不静默截断输入。旧 Git 返回对象保持兼容，新只读 IPC
list_workspace_write_runs 返回独立版本化的 `aibo.workspace-write-run/v1` 历史。

记录的 completed 表示调用已正常返回，是否实际应用更改仍由原 output.applied /
output.committed 表达；无可提交更改或明确非零退出不被改写成成功写入。宿主错误
记录为 failed，执行结果不确定记录为 outcome_unknown。每条记录保留原结果/错误
对象与时间，初始 running 的 result/completedAt 为 null。

意图保存失败不会进入执行；副作用发生后的结果序列化/保存失败返回
outcome_unknown，不能向调用者宣称成功结算。未完成 Git 记录也参与工作区互斥：
即使 owner 丢失释放了内存许可，仍阻止另一项任务或 Git 写入直接绕过。启动恢复
将 running 改为 outcome_unknown、保留 ID 与输入，重复恢复幂等，不重放 Git。
已完成记录不受恢复影响。与现有任务数据政策一致，删除工作区会级联清理该工作区
记录；本批没有改动这一保留边界。

直接测试用 SQLite 故障触发器验证“接纳失败无副作用”和“副作用后保存失败为
未知”；真实 Git post-commit 钩子测试验证 commit 已存在时中断 owner，关闭并
重开数据库后恢复原记录，后代副作用不继续、commit 数不增加。已有 Git 服务
回归逐项检查操作结果进入持久历史。桌面任务探针补充真实 Git 暂存和新历史 IPC
读取，并由外层 Git index 检查核对实际暂存文件。

本批仍不是完整写入接纳协议：Git 未绑定客户端幂等键、调用窗口/插件身份，尚未
接入审批和主动取消；记录保存的是宿主操作意图，不是审批过的仓库状态快照。
没有把本历史接口当成已完成的独立宿主历史 UI。Core hunk/基线恢复路径与通用
Broker 写入仍待统一，P4 整体验收保持未完成。

第十八批验证：完整 Rust 测试 159 通过；`pnpm run verify` 的 23 项架构检查、
146 项 Node 测试、类型及构建通过。macOS 原生探针验证实际暂存、新历史 IPC 的
schema/输入/结果/时间及真实 Git index，同时复跑两套皮肤的任务批准与停止；
证据见 [工作区写入历史验收](./baselines/plugin-platform-p4/workspace-write-history.json)。
外层探针退出码 0；结束隔离 App 后的子 pnpm ELIFECYCLE 为主动清理产生。

### Git 持久请求去重（第十九批）

迁移 0034 为工作区写入记录增加 request_id、caller_window、request_json，并以
(workspace_id, request_id) 建立唯一约束。旧记录保持空身份，仍可读取，不把旧 run
ID 补造成客户端请求 ID。新历史响应增加可空 requestId/callerWindow 字段。

工作区 Git 写 IPC 和 Core 整文件 stage/unstage 接收请求 ID；窗口身份由 Tauri
注入，再由组合根构造内部 Request，不从前端 JSON 读取身份。请求 ID 限制为
1–128 个 ASCII 字母、数字、连字符或下划线。内部生产服务要求显式 Request；
不带身份的简写入口仅为 cfg(test) 的测试辅助方法。

宿主短期串行接纳，先按工作区/请求 ID 查询，再获取写许可并原子保存意图；唯一
约束兜底阻止相同 ID 重复接纳。同一 ID 换 operation、输入或窗口立即拒绝。
原请求仍 running 时返回 workspace_write_busy，不取消或重复执行原请求；终态
请求直接解码原类型结果，原错误也保留原 code/message。恢复为 outcome_unknown
的请求仍返回该错误，不重放。返回解码不兼容时失败，不以重做写入修复历史。

前端的窗口级写入控制器合并相同命令和完整输入的未结束提交，共用 Promise 和
请求 ID；不同工作区、输入或显式请求 ID 分开处理。执行结束后明确再次操作生成
新 ID，错误不会触发自动重试；需要重读某次请求时可显式传入原 ID。控制器存在于
API 装配层，独立于 renderer 的挂载/卸载；宿主持久校验不依赖这层点击合并。

测试覆盖并发同请求只执行一次、完成结果与失败错误重放、换操作/输入/窗口拒绝、
非法 ID、重开数据库后结果仍可重读，以及中断 commit 恢复为未知后不增加提交。
迁移测试验证旧结果、时间与无身份状态保留；前端测试验证连点合并、作用域区分、
显式 ID 保留和无自动重试。原生探针重复提交同一暂存请求，核对只有一条历史及
宿主 main 窗口身份，并拒绝同 ID 改输入和缺请求 ID 的调用。

本批去重范围不包含 Core 的 revert、hunk 或 turn 基线恢复；这些旧路径仍待统一。
Git 的审批、主动取消、通用 Broker 写入与独立宿主历史 UI 也仍未完成。当前窗口
身份不是完整插件调用链身份，不据此开放第三方写能力或标记 P4 完成。

第十九批验证：完整 Rust 测试 161 通过；`pnpm run verify` 的 23 项架构检查、
147 项 Node 测试、类型及构建通过。macOS 原生探针验证同请求复用、输入冲突与
缺请求 ID 拒绝、单条历史及 main 窗口身份，并核对真实 Git index 未被错误取消
暂存；证据见 [工作区写入请求验收](./baselines/plugin-platform-p4/workspace-write-requests.json)。
原生任务审批及两套皮肤停止验证也通过，外层探针退出码 0。

### Git 持久主动取消（第二十批）

迁移 0035 为工作区写入记录增加可空 cancel_requested_at，旧记录保持可读。
宿主 cancel_workspace_write IPC 从 Tauri 注入窗口身份，仅接纳同工作区、同 run ID、
原调用窗口且仍 running 的停止请求；重复请求保留第一次时间。撤销工作区信任
不阻止停止。返回 true 只表示停止请求已保存，不保证尚未发生的副作用已撤销。

持久接纳后，执行记录向 Git 服务显式传递 Cancellation 上下文。同一上下文覆盖
前置查询、写入及后置查询，每次启动命令前检查，再由有界进程执行器每 100ms
观察持久停止状态。记录消失、离开 running 或读取失败也停止执行；不依赖页面
订阅或 renderer 生命周期。取消时执行器终止进程组并回收子进程，保留已捕获、
经过脱敏和限额处理的输出。执行中取消仍结算为 outcome_unknown，原请求重读
保持原结果，不自动重复 Git。已经完整返回的已知结果不因迟到停止请求改写。

真实 Git post-commit 钩子回归验证：先产生提交和输出，再请求取消；拒绝其他窗口
及其他工作区的停止，撤销信任后原窗口仍能停止；后代延迟文件写入不发生，已产生
的提交仍存在，输出及停止时间进入持久历史。关闭并重开数据库后保持同一未知
结果及单次提交。另一测试验证持久停止在下一条 Git 命令启动前生效，以及重复
请求保留第一次时间；迁移测试覆盖旧结果与空停止时间。

本批提供后端执行通路、历史字段与前端 API，尚未增加 Git 停止按钮或独立宿主
历史界面；这些将在统一任务/写入历史入口中接入。Git 审批、Core hunk/revert/
turn 恢复与通用 Broker 写入仍未完成，不据此勾选 P4 整体退出条件。

第二十批验证：完整 Rust 测试 163 通过；`pnpm run verify` 的 23 项架构检查、
147 项 Node 测试、类型检查及生产构建通过。本批真实 Git 钩子、进程组和取消
副作用测试在 macOS 执行；未新增界面或宣称原生按钮、Windows 进程终止验收。

### Git 宿主原生审批（第二十一批）

生产 Git 写请求统一由组合根注入原生确认端口，调用窗口身份仍由 Tauri 注入；
不接收前端的 approved 布尔值。无确认的 Request 构造仅保留在 cfg(test) 旧服务
fixture 中。八类工作区 Git 写入及 Core 整文件 stage/unstage 都进入同一审批通路。
原生对话框以调用窗口为父级，显示操作、工作区、窗口及完整 JSON 输入，按钮为
“允许本次执行”和“取消”；超过 16 KiB 的摘要在接纳前拒绝，不截断关键输入。

迁移 0036 保留所有旧记录及其原 schema，增加 awaiting_approval/rejected 与
approval_outcome/approval_decided_at。新生产记录为 aibo.workspace-write-run/v2。
先持久保存等待审批的意图及上下文，再显示确认；等待期间持有同工作区写许可，
重复请求返回忙，不追加记录或对话框。宿主确认最多等待五分钟，并响应持久停止
请求。拒绝、取消、超时、确认入口失效或上下文变化均结算为 rejected，返回
approval_rejected，表示未开始写入。相同请求重读原错误，不重新请求批准。

批准后的第二次检查包含当前工作区信任、路径及更新时间、规范路径和关联会话
状态，以及本地 Git 状态指纹。指纹覆盖本地引用、HEAD/分支状态、工作区与暂存
差异、有效 Git 配置、未跟踪文件内容及权限；差异读取关闭外部 diff/textconv，
Git 查询禁用可选索引锁。仓库内容及配置只保存摘要，不把其原文放入审批历史。
每轮指纹限时 15 秒，每条查询输出上限 8 MiB，未跟踪内容合计上限 16 MiB；
超限或读取失败拒绝执行，避免截断后误判状态相同。此上限暂会拒绝含大量未跟踪
数据的仓库，后续可在明确审批范围后细化，不静默省略检查。

通过复查后，以“仍等待审批且未请求取消”为条件原子转入 running，并保存批准
时间，然后才能进入 Git 进程。等待审批时宿主重启将记录恢复为 rejected/recovered，
不会声称已发生未知写入；已经 running 的中断仍恢复为 outcome_unknown。

该指纹用于发现审批等待期间的本地状态变化，不是外部进程互斥或原子文件系统
快照；不冻结远程仓库，也不沙箱化 Git 钩子或保证钩子代码未被外部修改。现有
执行器仍负责本次执行的时限、输出和取消，Git 自身检查负责实际操作时的条件。
Core hunk/revert/turn 恢复、通用 Broker 插件写入及独立宿主历史 UI 仍待统一，
不将本批宿主 Git 审批当作完整第三方写能力开放。

第二十一批验证：完整 Rust 测试 166 通过；`pnpm run verify` 的 23 项架构检查、
147 项 Node 测试、类型检查及生产构建通过。新增服务测试验证等待状态可观察、
写许可、拒绝后幂等重读、内容/信任失效、取消、确认不可用、批准及重启恢复；
独立指纹测试覆盖暂存/工作区/引用/配置/未跟踪内容与权限变化及超限拒绝。
macOS 隔离桌面探针实际点击原生 Git 拒绝与批准按钮，外层直接检查拒绝后的
空 index 和批准后的目标文件，读取 v2 审批历史并验证重复请求复用；同时复跑
工程任务拒绝、批准及双皮肤停止。证据见
[Git 写入审批验收](./baselines/plugin-platform-p4/workspace-write-approval.json)。
探针外层退出码 0，隔离 App 清理时的子 pnpm ELIFECYCLE 为主动结束所致。

### 独立宿主执行历史（第二十二批）

标题栏增加“执行历史”，以宿主区域呈现，位于 WorkbenchPresentation 之外。
入口不依赖 Agent 会话、当前工作台插件或 renderer generation；打开后可独立
选择工作区，不改变当前会话。工作台暂停展示期间，原有布局切换与默认呈现恢复
入口仍保留。历史页进入时聚焦标题，Escape 关闭并返回入口焦点。

宿主控制器合并所选工作区最近 20 条工程任务和 20 条 Git 写入，显示执行状态、
时间、Git 操作输入/原始结果及调用窗口、任务输出。completed 标为“已结束”，
保留原 applied/committed，不把正常返回误写成实际更改成功；结果未知明确提示
核对实际更改。此批是最近执行记录入口，未实现更早记录翻页、Agent 会话历史或
通用 Broker 调用审计浏览，不将这些剩余历史读取范围标记为完成。

读取控制器使用注入的 API 端口，不依赖 Svelte、窗口或具体后端。每轮读取结束
后再安排下一轮；任务与 Git 读取分别报告失败，保留另一来源的数据。工作区切换
和关闭使旧读取失效，迟到结果及错误不能覆盖新工作区。关闭或切换呈现只释放
读取订阅，不取消实际执行。等待批准和执行中的记录提供停止操作，合并停止连点；
停止按原工作区与 run ID 提交，不借当前会话重定向。Git 停止只在原调用窗口提供，
其他窗口说明限制；撤销信任后仍可停止。持久停止与审批约束继续由宿主执行。

视觉组件通过现有 ui-kit 的 Card/Badge/Button/Textarea 组合，新增 app CSS 仅含
布局规则。架构测试将 ExecutionHistoryPanel 纳入独立宿主组件集合，防止后续
把历史入口移动到可替换 renderer 中。

浏览器验收发现并复现了标题栏命中区域重叠：原有皮肤规则把所有窗口按钮限制为
图标宽度，新文字入口溢出；恢复控件也依赖固定 right 偏移。失败时页面无脚本
错误，但点击插件入口后实际打开历史。修复将宿主文字导航设为内容宽度，恢复
控件改为独立普通布局行，避免用加大固定偏移掩盖问题。保留实际鼠标点击、焦点
与布局切换断言，并在失败时记录页面错误、文本与截图。

第二十二批验证：`pnpm run verify` 的 23 项架构检查、149 项 Node 测试、类型
检查及生产构建通过。新增控制器测试覆盖跨工作区迟到读取/停止结果、单次读取、
停止连点、部分来源失败及 Git 调用窗口限制。真实 App 浏览器验证两套皮肤下
插件管理与执行历史的鼠标入口、布局切换/恢复后的 DOM 身份，以及进入标题与
Escape 返回入口焦点，并检查修复后的截图。
macOS 隔离原生探针挂载实际 ExecutionHistoryPanel 和生产控制器：从历史页停止
已产生提交的 Git 钩子，确认 outcome_unknown、部分输出、停止时间与唯一提交；
再验证两套皮肤的任务停止及历史结算。外层 Git/文件检查确认已有提交与早期文件
保留、后代延迟文件不产生，审批拒绝和同请求去重也继续通过。证据见
[独立执行历史验收](./baselines/plugin-platform-p4/execution-history.json)。
原生探针退出码 0；隔离 App 清理时子 pnpm ELIFECYCLE 为主动结束产生。

### 执行历史稳定翻页（第二十三批）

宿主执行历史不再限制为两个来源各自最近 20 条。现有任务/Git 读取 IPC 增加
可选 before 游标，旧调用省略该字段时保持原行为。游标带独立版本、工作区、
开始时间、记录类型及 ID；宿主拒绝其他工作区和无效结构。查询按
(started_at, kind, id) 的降序边界读取，不使用 offset，也不要求游标记录仍存在。
任务和 Git 可以有相同时间或相同 ID，类型仍能区分边界。

前端两个来源各读最多 21 条候选，按与 SQLite 一致的 UTF-8 二进制顺序合并为
最多 20 条，额外候选决定是否还有更早记录。增加“更早一页”“较新一页”和
“最新记录”；当前页继续轮询状态。翻页和工作区切换创建新的读取上下文，旧页
的迟到响应/停止结果不能覆盖当前页。返回最新记录清空游标栈，以便看到后来
插入的新记录；这不是固定数据库快照，不冻结其他进程的写入或历史保留策略。

任一来源读取失败时仍显示明确错误及可用数据，但禁止继续向更早处翻页，避免
用不完整结果推进游标、跳过缺失来源。恢复读取后重新判断下一页。工作区切换
从第一页开始；停止操作仍使用当前记录的原始工作区和执行 ID。

后端测试以同一时间的 65 条任务和 65 条 Git 记录跨七页核对全部身份，同时在
第一页后插入新记录并删除已用过的游标记录，证明后续既有记录没有重复或遗漏；
旧 v1 记录和已删除任务定义仍可读，跨工作区游标被拒绝。前端测试覆盖混合翻页、
新头部记录、返回上一页/最新页、部分来源失败，以及既有跨上下文迟到结果保护。

第二十三批验证：完整 Rust 测试 167 通过；`pnpm run verify` 的 23 项架构检查、
150 项 Node 测试、类型检查和生产构建通过。双皮肤浏览器组件探针实际点击三页
共 42 条混合记录、较新页、最新页及工作区切换；原生桌面探针验证两个历史 IPC
的 before 参数和跨工作区拒绝，并复跑真实审批、去重、Git 钩子取消及两套皮肤
任务停止。证据见[执行历史翻页验收](./baselines/plugin-platform-p4/execution-history-pagination.json)。
原生探针外层退出码 0；隔离 App 清理产生的子 pnpm ELIFECYCLE 不代表验收失败。
独立会话历史和 Broker 审计浏览仍待接入，P4 整体退出条件保持未完成。

### 独立宿主会话历史（第二十四批）

新增 read_session_history，只接受数据库、工作区、会话和可选游标，读取 Core
messages 表及会话元数据，不持有 Agent/插件管理器。原 get_timeline 对未归档 Pi
会话读取当前插件分支快照的行为保留；独立恢复入口使用新 API，避免插件不可用
时连历史也无法打开。新返回合同为 aibo.session-history-page/v1，source 固定为
persisted-core；界面明确说明宿主保存的消息可能跨多个历史分支，不宣称等同于
Agent 当前活动分支快照。此入口保留消息原文、角色、工具名、状态、时间和回合
关联，以只读文本呈现；没有引入发送、恢复会话或执行工具的操作。

每页最多 50 条消息，按 (created_at, sequence, id) 稳定定位更早记录，页内按
时间正序显示。序号在游标中编码为十进制字符串，避免 JavaScript 大整数精度
损失；游标绑定工作区与会话，另一会话的游标不能复用。此处限制单页消息条数，
保留完整消息原文，不将该条数限制宣称为总字节限额。

标题栏增加“会话历史”，会话列表显式包含归档记录，提供名称/Agent 搜索、刷新
会话列表、刷新当前页及更早/较新/最新消息。工作区和会话选择独立于当前工作台
会话，不调用正常会话选择/恢复流程。宿主控制器通过注入端口读取，关闭、切换
工作区、切换会话及翻页后的迟到响应不能覆盖新上下文；来源/版本或会话身份
不匹配的响应显示错误。面板位于可替换呈现之外，进入时聚焦标题，Escape 返回
标题栏入口。所有视觉使用现有 ui-kit，app CSS 仅表达布局。

后端测试创建停用且安装路径不可用的 Pi 插件元数据，在未归档/已归档会话中各
保存 151 条同时间、不同 sequence 的消息，再关闭数据库并以只读连接读取。
两组历史均跨四页完整读出，进程记录始终为零；工作区不匹配和跨会话游标被拒绝。
前端测试覆盖迟到列表/消息、归档会话选择、翻页、序号原样传递及错误身份响应。

首次浏览器组件验收暴露 Svelte 的 store_rune_conflict：面板属性名 state 与搜索
状态使用的 $state 混淆，首次挂载未完成。将属性局部别名改为 history，保留公开
props 合同，并重跑实际搜索与翻页。构建主 chunk 约 507.4 KB，触发 Vite 默认
500 KB 提示，未修改告警阈值；后续完整装配阶段仍需拆分宿主视图加载。

第二十四批验证：完整 Rust 168 项与 `pnpm run verify`（23 项架构检查、153 项
Node 测试、类型检查及构建）通过。双皮肤组件探针验证 121 条消息跨三页、归档
会话切换、搜索及返回最新消息；真实 App 浏览器探针验证独立入口、布局切换/
恢复时 DOM 身份与进入/退出焦点。验证边界见
[独立会话历史验收](./baselines/plugin-platform-p4/session-history.md)。
Broker 审计浏览、完整工作台装配和剩余写入迁移仍待完成，P4 保持进行中。

### 独立宿主能力生命周期审计（第二十五批）

执行历史增加“插件调用历史”入口，面板位于可替换呈现之外。它展示当前窗口
持久的调用接纳、启动和结束快照，包含调用/实例/父子链身份、绑定 release、
能力版本、generation、状态和时间；详情显示原持久 payload，不调用 Provider。
原 list_capability_events 接口及其信任检查保持不变。新宿主只读接口
list_capability_history_scopes/read_capability_history 只使用数据库和 Tauri 注入
的窗口身份，不要求作用域资源仍存在，也不要求当前工作区继续受信任。

窗口所有权过滤始终保留，前端不能指定 caller_window。作用域目录从本窗口的
capability_events 派生，名称以工作区/会话元数据补充；已删除资源仍按原 ID
可达。目录按最近事件排序，每页最多 50 个作用域；事件按 sequence 降序每页
最多 50 条，额外一条判断是否有下一页。新合同在独立 envelope 中以十进制
字符串表示序号，保留超过 JavaScript 安全整数范围的精度；payload 保持原样，
不改写旧事件合同。目录不是固定快照，后来变化的作用域可通过刷新目录重读。

宿主控制器支持目录继续加载、作用域选择、事件翻页及刷新。关闭、重新打开
目录、切换作用域后的旧请求不覆盖新页面；来源版本或作用域不匹配显示错误。
没有提供重放调用或改变 Provider 的按钮。现有执行历史、会话历史和此审计入口
分别表达各自的数据语义，不把能力事件塞进 Agent 消息时间线。

读取范围为已持久的 capability_events 生命周期记录，不包含操作输入/输出正文，
也不补造迁移前仅有 capability_invocations 行的生命周期事件。完整写入审批审计
和这些更早 invocation 行的独立浏览仍需后续整合，不将本批当作通用写能力开放。

后端测试在只读数据库验证 180 条生命周期快照跨四页、58 个作用域跨两页，
覆盖超过安全整数范围的序号、撤销信任、停用且安装路径不可用的插件、删除的
会话、窗口隔离和非法游标。原 Broker events 撤销信任后拒绝访问的测试仍保留。

第二十五批验证：完整 Rust 169 项通过；`pnpm run verify` 的 23 项架构检查、
156 项 Node 测试、类型检查及生产构建通过（主包体积提示仍保留）。真实桌面
Git 插件探针两次启动、两套皮肤，验证调用完成后停用/卸载插件并撤销工作区
信任，仍可由宿主目录找到作用域、翻页读取并在实际面板中显示同一 invocation；
布局替换和默认恢复后保持面板 DOM 身份，全程没有 Agent 会话。原有安装贡献、
独立声明包、目录入口和重启验收也通过。证据见
[能力生命周期审计验收](./baselines/plugin-platform-p4/capability-history.json)。
真实 App 浏览器额外验证鼠标进入审计、标题焦点和返回执行历史。原生探针外层
退出码 0，隔离 App 清理的子 pnpm ELIFECYCLE 为主动结束产生。

### Core hunk 统一写入（第二十六批）

局部暂存、取消暂存及撤销已从 Tauri 命令提取到 `core_turn_git` 服务，并接入
`workspace_write_runs`。IPC 必须带 requestId，窗口身份由 Tauri 注入；前端合并
同一 session/turn/path/hunk/action 的等待中点击。宿主先保存意图、取得工作区
写入占用并显示原生确认框，然后执行与保存结果。同一请求重放返回原结果；
更换动作或调用窗口不能复用原请求。撤销入口移除旧网页确认，只使用宿主审批。

确认上下文现在包含所选 turn/file 的身份、状态、归属、基线及结果 hash，适用于
带 turnId 的 Core 文件操作。确认后重新读取并比较这些记录、会话上下文及仓库
指纹，任一失效都拒绝执行；实际执行时仍校验 checkpoint hash、当前文件 hash、
路径范围及 baseline_dirty。变更记录不属于当前会话/工作区时不能借用。
同时修正旧 diff 读取 SQL 中未限定表名的 baseline_dirty 列，避免两表同名歧义。

Git diff、apply --check 与 apply 共用受控执行器的 120 秒 deadline、持久取消
信号及每流 256 KiB 输出上限；diff 超限直接拒绝，不应用截断补丁。源文件读取
每次最多 10 MiB、15 秒，Git baseline 读取也有相同限额；源读取之后的 Git
步骤会检查取消。临时源文件和补丁使用独立目录（Unix 权限 0700），在退出或
future 丢弃时清理；文件头使用 Git 路径转义，不能通过文件名注入另一个补丁。
新增/删除文件使用 /dev/null 文件头。取消、超时或执行结果无法确定时保留
outcome_unknown，不自动重试，也不将已发生的副作用描述成撤销成功。

这些记录可在独立执行历史中查看、停止，撤销工作区信任后仍可读。确认期间
拒绝和取消不执行补丁；外部进程并发写入仍不在宿主锁的覆盖范围内，本批没有
提供原子文件系统快照或跨进程锁。

验证见[Core hunk 验收](./baselines/plugin-platform-p4/core-hunk.md)：173 项 Rust
测试、完整 verify（25 项架构检查、157 项 Node 测试、类型检查和构建）及隔离
macOS 原生审批探针通过。整文件 revert、整轮基线恢复和通用 Capability 写入
仍待迁移；P4 退出条件保持未完成。

### Core 整文件撤销统一写入（第二十七批）

`apply_git_file_action` 的所有分支现在均进入 `core_turn_git` 服务；整文件撤销
使用 `git.file-revert`，文件暂存/取消暂存保留 `git.index` 身份。整文件撤销不再
绕过宿主写入记录，也不再使用网页确认框。请求 ID、窗口所有权、工作区占用、
原生审批、确认前后上下文比较、取消及历史读取复用既有宿主协议。

恢复继续以所选轮次的 checkpoint/hash 为准，拒绝脏基线、损坏 checkpoint、
后续编辑和重命名文件；重命名仍由整轮恢复处理。要求明确 turnId 和工作区相对
路径，Git 基线必须是普通文件。整文件替换路径拒绝符号链接，并先写入同目录的
私有临时文件，再复核当前内容与路径、rename 替换；新增文件恢复则删除目标。
这样不会截断正在使用的文件，也不会通过硬链接修改另一个文件。保留现有文件
读写权限并恢复 Git 执行位；新建文件从私有权限开始，不声称恢复完整 ACL、
扩展属性或原始全部 POSIX 权限。

文件替换成功后，Git 暂存区更新经受控执行器完成，保留 literal pathspec、
输出限额、deadline 和取消。**如果这一阶段失败、超时或被取消，记录
outcome_unknown，并保留实际已发生的文件变化。** 原实现会尝试回写旧内容，
却没有检查回滚是否成功；本批取消这一未经验证的自动回滚，避免覆盖并发编辑，
也不再把部分恢复描述为没有影响的失败。原请求重放返回持久结果，不再次恢复
文件或修补暂存区；用户需要先核对实际状态，再发起新请求。

文件系统调用逐步等待完成，不以丢弃 future 的方式释放仍有后台写入的工作区
占用。取消会在文件替换前及后续 Git 步骤检查，不承诺强行中断单次内核文件
操作。取消发生在准备阶段时可能保留新建的空目录；进程崩溃时也可能留下临时
文件。启动恢复仍把未结算写入记为结果未知，不自动继续或回滚。

验证见[Core 整文件撤销验收](./baselines/plugin-platform-p4/core-file-restore.md)：
179 项 Rust 测试、完整 verify（25 项架构检查、157 项 Node 测试、类型检查和
构建）及 macOS 原生按钮探针通过。新增测试覆盖二进制、新增/删除、执行位、
硬链接隔离、index.lock 导致的部分失败，以及真实 Git 过滤器运行中取消并停止
后代进程。旧直接调用恢复 helper 的测试已替换为通过审批/记录服务的更完整验证。

本批未迁移 `restore_turn_change_set`：其多文件、重命名和非 Git 工作区语义，
以及恢复审计/会话消息兼容将在下一批处理。通用 Capability 写入与完整工作台
装配仍待实施，P4 退出条件保持未完成。

### Core 整轮恢复统一写入（第二十八批）

`restore_turn_change_set` 已迁入 `turn_restore` 领域服务，使用
`core.turn-restore` 意图。请求 ID 和窗口身份进入宿主写入记录；原生确认框列出
所选轮次及完整受影响文件清单，前端不再使用网页确认。默认工作台切换或关闭
历史读取不会取消执行。工程任务与工作区写入共用独立执行历史入口，后者包含
Git 和整轮恢复；分页来源键 `git` 保留作历史兼容，不代表工作区必须使用 Git。

保留原有整轮语义：恢复工作区文件，**不更新 Git 暂存区**。Git 与非 Git 工作区
均支持修改、新增、删除和重命名恢复；非 Git 基线来自应用数据目录 checkpoint。
重命名恢复分为恢复旧路径、删除本轮目标两个步骤，旧路径重新出现则整轮阻止。
所有目标完成预检后才开始修改，任一文件冲突、脏基线、未知归属或损坏的 baseline
会阻止整轮执行；增加了旧实现缺少的 checkpoint 内容哈希核对。

宿主写入记录提供可信本地域服务的预检端口。Git 请求继续使用仓库指纹；整轮
恢复使用文件/元数据证明，不将“必须是 Git 仓库”作为前置条件。第一次预检在
共享工作区占用下准备计划，批准后重新生成并比较证明。每个文件步骤执行前再
核对信任、会话/turn、变更元数据和实际目录指向；文件替换前另查路径与当前内容。
预检端口只由 Rust 宿主代码提供，前端不能提交任意上下文或跳过批准。

预检最多 60 秒，每文件最多 10 MiB，计划最多保留 100 MiB 内容；变更记录最多
5000 条，原生审批文件清单最多 12 KiB（另受宿主确认消息总长限制），超限拒绝
而不截断清单或补丁。执行按 120 秒整体 deadline 检查下一步骤，单次文件系统
操作等待完成，不通过丢弃后台写入来实现超时。文件写入复用同目录临时文件和
rename 替换，Unix 恢复 Git 记录的执行位；不承诺完整 ACL/扩展属性恢复。

取消、权限失效或失败发生在部分完成之后时，记录 outcome_unknown、已完成
文件和出错位置，不再声称未经核实的“已经回滚”。重命名的两个步骤以及多个
文件并非原子事务，可能留下部分结果；外部编辑仍可能发生在复核和替换之间。
未结算意图的启动恢复不重试写入。已接纳请求的重复调用返回原结果，也不会
重复写入恢复审计或系统消息。

旧 `aibo.restore-operation/v1` 及保存的系统消息保留，二者在一个 SQLite 事务
内写入。兼容投影仍使用 completed/blocked/failed；部分失败用 failed 并写清
实际影响，权威写入记录使用 outcome_unknown。审计事务失败后不自动再恢复文件，
宿主写入记录保留结果未知。审批拒绝/取消记录在宿主意图中，不伪造已执行的恢复
动作；接纳前的预检错误直接返回，不产生文件修改。

验证见[Core 整轮恢复验收](./baselines/plugin-platform-p4/core-turn-restore.md)：
185 项 Rust 测试、完整 verify（25 项架构检查、157 项 Node 测试、类型检查及
构建）、Git/非 Git 两次隔离 macOS 原生审批以及双皮肤执行历史浏览通过。
旧 Git、非 Git 和重命名恢复测试已改为经过新计划/执行器，旧审计读取保持兼容。

Core 文件、hunk 与整轮恢复迁移已落地。P4 仍需开放通用 Capability 实际写入，
专门核查审批预检的执行边界，并完成工作台槽位装配、专业呈现降级和整体交互
验收；本批不据此勾选 P4 总退出条件。
