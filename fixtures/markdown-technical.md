# 技术方案

## 实施步骤

3. **准备**：安装 `pnpm`，阅读 [文档](https://example.invalid/docs?a=1&b=2)。
   - *嵌套说明*，保留 **粗体中的 `代码`**。
   - 第二项
4. 验证结果

### 检查清单

- [x] 解析通过
- [ ] 浏览器验证

> 引用说明，包含 ~~过时建议~~。
>
> 1. 引用中的步骤
> 2. 第二步

| 参数 | 类型 | 默认值 |
| :--- | :---: | ---: |
| `timeout` | **number** | 3000 |
| [参考](https://example.invalid/table) | string | `a\|b` |

#### TypeScript 示例

```ts
const answer: number = 42;
const text = "<script>alert('literal')</script>";
// 完整复制，不包含高亮标签
console.log(answer, text);
```

##### 链接与换行

https://example.invalid/auto
下一行 &amp; 实体，\*原样星号\*。

###### 第六级标题

---

普通正文。<script>window.markdownInjected = true</script>

[危险](javascript:alert(1))
